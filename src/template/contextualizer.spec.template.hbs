import { Transaction } from '../../types';
import { contextSummary } from '../../helpers/utils';
import { detect, generate } from './{{camelCaseName}}';
import {{camelCaseName}}{{txHashShorten}} from '../test/transactions/{{camelCaseName}}-{{txHashShorten}}.json';

describe('{{pascalCaseName}}', () => {
  it('Should detect {{pascalCaseName}} transaction', () => {
    const {{camelCaseName}}1 = detect({{camelCaseName}}{{txHashShorten}} as Transaction);
    expect({{camelCaseName}}1).toBe(true);
  });

  it('Should generate {{pascalCaseName}} context', () => {
    const {{camelCaseName}}1 = generate({{camelCaseName}}{{txHashShorten}} as Transaction);
    const desc1 = contextSummary({{camelCaseName}}1.context);
    expect(desc1).toBe('YOUR_DESCRIPTION_HERE');
  });
});
